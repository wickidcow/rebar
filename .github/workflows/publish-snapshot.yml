name: Maven Snapshot Publishing Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing is disabled on this fork
        run: |
          echo "Maven Central snapshot publishing is intentionally disabled."
          echo "Use 'Build Rebar 26.2 Test JAR' to get a downloadable test JAR."
