name: Maven Stable Publishing Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing is disabled on this fork
        run: |
          echo "Maven Central stable publishing is intentionally disabled."
          echo "No Sonatype username, password, or PGP key is required for test builds."
