name: Documentation Publishing Disabled During Port

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Documentation publishing is disabled during 26.2 testing
        run: echo "Docs publishing is disabled so fork CI stays focused on test JAR builds."
