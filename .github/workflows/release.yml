name: Automatic Release Publishing Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Release publishing is disabled on this fork
        run: |
          echo "Automatic tagged releases are intentionally disabled during 26.2 testing."
          echo "Use 'Build Rebar 26.2 Test JAR' for direct test JAR downloads."
